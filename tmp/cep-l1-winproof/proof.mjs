import fs from 'node:fs';
import path from 'node:path';
import os from 'node:os';
import {createHash} from 'node:crypto';
import {ConptyTerminalManager} from './conpty-terminal-manager.mjs';
const root=path.resolve(process.argv[2]||'artifact');fs.mkdirSync(root,{recursive:true});const helper=path.resolve(root,'cep-win-sidecar.exe');const manager=new ConptyTerminalManager({helperPath:helper});const cases=[];const events=[];
for(const ev of ['control','diagnostic','exit','error-event'])manager.on(ev,e=>events.push({kind:ev,...e}));
const record=(id,pass,details={})=>{cases.push({id,status:pass?'PASS':'FAIL',...details});if(!pass)process.exitCode=1};const sleep=ms=>new Promise(r=>setTimeout(r,ms));async function until(fn,ms=15000,debug=null){const end=Date.now()+ms;while(Date.now()<end){const v=fn();if(v)return v;await sleep(100)}throw new Error('TIMEOUT'+(debug?':'+JSON.stringify(debug()):''))}function text(id){return Buffer.from(manager.output(id,0).dataBase64,'base64').toString('utf8')}async function awaitExit(id,ms=15000){return until(()=>{const s=manager.session(id);return s&&['completed','error'].includes(s.state)?s:null},ms,()=>({session:manager.session(id),output:text(id).slice(-5000),events:events.filter(e=>e.sessionId===id).slice(-30)}))}async function awaitReady(id,ms=15000){return until(()=>{const s=manager.session(id);if(s&&['completed','error'].includes(s.state))throw new Error('READY_ABORTED:'+JSON.stringify(s));const raw=manager._session(id);return raw?.controlHost&&raw?.controlPort&&raw?.inputHost&&raw?.inputPort&&raw?.epoch?s:null},ms,()=>({session:manager.session(id),events:events.filter(e=>e.sessionId===id).slice(-30)}))}function inputTelemetry(id){const rows=events.filter(e=>e.sessionId===id&&e.event?.event==='input-write').map(e=>e.event);return {events:rows,bytes:rows.reduce((n,e)=>n+Number(e.bytes||0),0),written:rows.reduce((n,e)=>n+Number(e.written||0),0),allOk:rows.length>0&&rows.every(e=>e.ok===true)}}async function runCase(name,fn){try{await fn()}catch(e){record(name,false,{error:String(e?.stack||e),events:events.slice(-50)})}}
const systemRoot=process.env.SystemRoot||'C:\\Windows',ps=path.join(systemRoot,'System32','WindowsPowerShell','v1.0','powershell.exe'),cmd=path.join(systemRoot,'System32','cmd.exe'),whoami=path.join(systemRoot,'System32','whoami.exe'),cwd=path.resolve(root,'cwd-proof');fs.mkdirSync(cwd,{recursive:true});
record('ENV-WINDOWS',process.platform==='win32',{platform:process.platform,release:os.release(),arch:os.arch(),node:process.version});record('PROVIDER-AVAILABLE',manager.available(),{descriptor:manager.descriptor()});
await runCase('TERM-PS-PROFILE',async()=>{const id='cep-proof-powershell';manager.open({sessionId:id,label:'PowerShell',executable:ps,args:['-NoLogo','-NoProfile','-Command',"Write-Output 'CEP_PS_OK'; Write-Output $env:CEP_PROOF_ENV; Write-Output (Get-Location).Path; exit 7"],cwd,env:{CEP_PROOF_ENV:'CEP_ENV_OK'},cols:100,rows:28});const done=await awaitExit(id);const out=text(id);record('TERM-PS-PROFILE',out.includes('CEP_PS_OK')&&out.includes('CEP_ENV_OK')&&out.toLowerCase().includes(cwd.toLowerCase())&&done.exitCode===7,{exitCode:done.exitCode,output:out.slice(-3000)})});
await runCase('TERM-CMD-PROFILE',async()=>{const id='cep-proof-cmd-profile';manager.open({sessionId:id,label:'cmd.exe',executable:cmd,args:['/D','/Q','/C','echo CEP_CMD_PROFILE_OK & exit /b 5'],cols:90,rows:25});const done=await awaitExit(id);const out=text(id);record('TERM-CMD-PROFILE',out.includes('CEP_CMD_PROFILE_OK')&&done.exitCode===5,{exitCode:done.exitCode,output:out.slice(-2000)})});
await runCase('TERM-RAW-IO-RESIZE-RESTART',async()=>{const id='cep-proof-raw';manager.open({sessionId:id,label:'cmd.exe raw',executable:cmd,args:['/D','/Q'],cols:90,rows:25});const r1=await awaitReady(id);await manager.resize(id,132,42);await until(()=>manager._session(id).lastResize,5000);record('TERM-RESIZE',manager._session(id).lastResize?.ok===true,{resize:manager._session(id).lastResize});const payload=Buffer.from('echo CEP_RAW_IO_OK\r\nexit /b 0\r\n','utf8');await manager.write(id,payload);const d1=await awaitExit(id);await sleep(300);const out1=text(id),iw1=inputTelemetry(id);record('TERM-RAW-IO',d1.exitCode===0&&out1.includes('CEP_RAW_IO_OK')&&iw1.written===payload.length&&iw1.allOk,{exitCode:d1.exitCode,payloadBytes:payload.length,inputWrite:iw1,output:out1.slice(-3000)});const oldEpoch=r1.epoch;await manager.restart(id);const r2=await awaitReady(id);const payload2=Buffer.from('echo CEP_RESTART_OK\r\nexit /b 0\r\n','utf8');await manager.write(id,payload2);const d2=await awaitExit(id);await sleep(300);const out2=text(id),iw2=inputTelemetry(id);record('TERM-RESTART-STABLE-ID',r2.id===id&&r2.restartCount===1&&r2.epoch!==oldEpoch&&d2.exitCode===0&&out2.includes('CEP_RESTART_OK')&&iw2.written===payload2.length,{sessionId:r2.id,restartCount:r2.restartCount,oldEpoch,newEpoch:r2.epoch,exitCode:d2.exitCode,inputWrite:iw2,output:out2.slice(-2500)})});
await runCase('TERM-ARBITRARY-EXECUTABLE',async()=>{const id='cep-proof-arbitrary';manager.open({sessionId:id,label:'whoami.exe',executable:whoami,args:[],cols:80,rows:24});const done=await awaitExit(id);const out=text(id);record('TERM-ARBITRARY-EXECUTABLE',done.exitCode===0&&out.trim().length>0,{exitCode:done.exitCode,output:out.slice(-1000)})});
await runCase('TERM-ERROR-TRUTH',async()=>{const id='cep-proof-missing';manager.open({sessionId:id,label:'missing',executable:path.join(root,'definitely-missing.exe'),args:[]});const done=await awaitExit(id);record('TERM-ERROR-TRUTH',done.state==='error'&&done.error?.code==='CREATE_PROCESS_FAILED',{state:done.state,error:done.error,exitCode:done.exitCode})});
const descriptor=manager.descriptor();record('NO-ALLOWLISTS',descriptor.commandAllowlist===false&&descriptor.executableAllowlist===false,{descriptor});const environment={utc:new Date().toISOString(),platform:process.platform,release:os.release(),version:os.version?.()||null,arch:os.arch(),node:process.version,runnerImage:process.env.ImageOS||null,runnerName:process.env.RUNNER_NAME||null};fs.writeFileSync(path.join(root,'terminal-test-results.json'),JSON.stringify({mission:'MISSION_WINDOWS_NATIVE_PLATFORM_TERMINAL_CONVERGENCE',classification:'MANAGED_WINDOWS_CONPTY_EVIDENCE_ONLY__NOT_INTERACTIVE_PLATFORM_ACCEPTANCE',rawInputEncoding:'UTF-8 bytes from xterm/TerminalRuntimeCapability without command parsing',cases,events},null,2));fs.writeFileSync(path.join(root,'environment.json'),JSON.stringify(environment,null,2));fs.writeFileSync(path.join(root,'terminal-provider-descriptor.json'),JSON.stringify(descriptor,null,2));const files=fs.readdirSync(root).filter(n=>fs.statSync(path.join(root,n)).isFile()).sort();fs.writeFileSync(path.join(root,'sha256.txt'),files.map(n=>`${createHash('sha256').update(fs.readFileSync(path.join(root,n))).digest('hex')}  ${n}`).join('\n')+'\n');console.log(JSON.stringify({pass:cases.every(c=>c.status==='PASS'),cases},null,2));if(cases.some(c=>c.status!=='PASS'))process.exit(1);
