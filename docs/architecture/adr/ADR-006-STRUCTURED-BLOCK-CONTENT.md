# ADR-006 — Structured Block Content

Status: **PROPOSED**. Runtime lessons use ordered, addressable, typed/schema-versioned blocks with validated payloads, provenance, bidi metadata, and nesting rules. Markdown is import/export only. Arbitrary JSON and raw executable HTML are rejected.

