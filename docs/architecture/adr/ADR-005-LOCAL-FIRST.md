# ADR-005 — Local-first Deployment

Status: **PROPOSED**. Core v1 operates Browser -> local app -> PostgreSQL/local storage without public Internet and binds locally by default. External source lookup and manual ChatGPT Plus processing remain explicit user-controlled actions.

