# Task 006 Repository Foundation Scope Handoff

Status: **scope handoff only — not an implementation prompt and not authorization**.

Task 006 may establish the coherent repository foundation selected in Task 004: Laravel/PHP backend, Vue 3 + TypeScript frontend through Inertia.js, PostgreSQL, HTML/CSS/Tailwind, WSL2 + Docker Compose local environment, PHP/Laravel and frontend tests. Before scaffolding it must inspect the actual environment, select currently supported compatible versions, lock all dependencies, and record exact versions; Task 004 selects no versions.

Minimum foundation outcomes are a single deployable Modular Monolith, module-boundary conventions, one local owner authentication flow, secure session/CSRF defaults, audit and identifier primitives, PostgreSQL connectivity, local blob abstraction contract, database-backed queue foundation, health/diagnostic command, Arabic/English shell baseline, and repeatable tests.

Task 006 must not implement source ingestion, publication workflows, curriculum content, mastery, Enterprise Catalog, Scenario Studio, simulator rules, evidence workflows, Manual AI result processing, VS-001, real connectors, or broad empty CRUD. It must not introduce Redis, Kafka, Kubernetes, microservices, graph/search clusters, automated/provider/local AI, a separate frontend deployment unit, or a second backend language.

Acceptance evidence must include environment inventory, selected versions and compatibility rationale, lockfiles, clean build/test commands, authentication/session/security tests, module-dependency enforcement, local-only exposure check, database migration safety for foundation tables, and explicit residual limitations. The candidate stop gate is `STOP-REPOSITORY-FOUNDATION-006`.

