# VS-002 testing

Run PHP format, PHPStan, architecture tests, all PHP tests against PostgreSQL, migration rollback/reapply, Prettier check, ESLint, vue-tsc, Vitest and Vite build. Run Composer and npm locked audits and the bounded secret scanners.

The parameterized engine test covers all twelve scenario cases. Lifecycle tests cover actor-bound idempotency, finding creation, immutable remediation, verification links, spoof rejection and revision-pinned replay. Workspace tests cover protected routes, validation, structured practice and real lesson transitions. Vitest verifies the stored XSS marker remains inert.

Docker was checked once and is unavailable in this environment; do not report Docker PASS. Native PostgreSQL is the tested database.
