# 09_REMEDIATION_REQUIREMENTS_INPUT__NOT_EXECUTION_PLAN

**Project:** Cybersecurity Education Platform (`CEP`)  
**Route:** `PERSONAL:CEP`  
**Role:** W02 VISUALIZE — Deep Information-Visualization / Visual-System / Interaction-Design Auditor  
**Audit type:** READ-ONLY / BLIND-FIRST / exact-candidate assurance  
**Repository:** `hamad933/Cybersecurity-Education-Platform`  
**Branch:** `work/cep-w02-library-work-visual-r01`  
**Verified SHA:** `ca36e75c116a9ba00b5d25d358bd68c10990bd6e`  
**Verified parent:** `7fa8714dc6d0beec6ec77ba8a673140301b066cf`  
**Baseline drift:** `NONE / NO_BASELINE_DRIFT`  
**Product mutation:** `NONE`  
**Writer dispatch:** `NONE`  
**Acceptance / merge / release / deploy:** `NONE`


## 1. Status

هذا الملف **Remediation Requirements Input فقط**. ليس execution plan، وليس writer prompt، ولا يحدد branch أو sequencing للتنفيذ، ولا يوسع أي write scope.

## 2. Tree requirements

- MUST preserve containment-only semantics.
- MUST prove lawful Domain → Cluster → Capability → KU when authoritative data supplies it.
- MUST not silently discard valid multi-parent placements.
- MUST distinguish structural levels beyond color alone.
- MUST use human labels only from authorized provenance.
- MUST avoid duplicate technical-fallback rendering.
- MUST keep expand/collapse accessible and visibly attached to parent identity.
- MUST prioritize hierarchy over repetitive `contains` tokens.
- MUST distinguish active/current/focus/selection truthfully.
- MUST close long-tree and ~1024 behavior with direct runtime evidence.

## 3. Path requirements

- MUST NOT call prerequisite topology canonical authored path order without authority.
- MUST project canonical pathway/order only from an authorized provider.
- MUST explicitly expose prerequisite-derived fallback when no path authority exists.
- MUST surface cycles as cycles, not normal final stages.
- MUST avoid lexical/tie-break ordering looking like authored sequence.
- MUST render truthful connectors between actual related nodes/groups.
- MUST make fork/join semantics perceptible when exact relations prove them.
- MUST keep selection separate from current/progress state.
- MUST replace raw technical endpoint copy as primary relation comprehension.
- MUST use CENTER space as a journey visualization, not a narrow stage strip.
- MUST prove local overflow and context access at ~1024.

## 4. Graph requirements

- MUST preserve semantic `from → to` direction independent of RTL.
- MUST preserve unique canonical node identity.
- MUST provide a semantic key for typed visual encodings.
- MUST keep edge labels readable at default working fit.
- MUST reduce material node/edge/label collisions at representative density.
- MUST make selected relation and its endpoints coherent with layout focus.
- MUST distinguish structural node roles using non-color cues.
- MUST make non-focus/isolated node treatment explicit.
- MUST close node-selected focus and pan/zoom/FIT runtime evidence.
- MUST not invent relationships to improve topology appearance.

## 5. Canvas requirements

- MUST remain representation-only unless Saved Map authority later changes the product contract.
- MUST NOT persist visual positions through localStorage or canonical Curriculum records.
- MUST define truthful provenance for the initial spatial arrangement; Graph-layout reuse must not masquerade as spatial meaning.
- MUST provide a deliberate camera-pan model distinct from node dragging.
- MUST make relationship type interpretable in Canvas.
- MUST provide legend/equivalent semantic key.
- MUST visibly distinguish selected edge from Overlay emphasis.
- MUST subordinate ephemeral coordinates to actual object/relationship meaning.
- MUST prove pointer/keyboard movement and canonical invariance.
- MUST not implement Saved Map in this audit scope.

## 6. Map / View / Filter / Overlay requirements

- MUST keep `MAP != VIEW != FILTER != OVERLAY`.
- MUST distinguish world/Map identity from the `all` filter in Arabic copy and IA.
- MUST keep unavailable layers truthful: NO_DATA / NO_AUTHORITY / unsupported / invalid schema.
- MUST never infer zero, mastery, progress, evidence or coverage from absence.
- MUST preserve route-state normalization and Back/Forward behavior.
- MUST clear invalid selections when View/filter removes the target.

## 7. Representative data requirements

Evidence fixture MUST exercise, lawfully:
- multiple domains.
- multiple clusters.
- multiple capabilities.
- multiple KUs.
- 4-level hierarchy.
- canonical/authorized human labels.
- prerequisites.
- branch/join.
- cycle.
- no-path state.
- focused Graph node with inbound/outbound neighbors.
- isolated node.
- all relation types actually claimed by runtime.
- long mixed Arabic/English labels.
- density sufficient for collision review.

Fixture evidence MUST be labeled as fixture evidence and MUST NOT be represented as a full canonical runtime import unless that import is separately proven.

## 8. Shared dependency requirements

- shared shell ownership must be handled by the shared integration owner.
- canonical Path order must be handled by its authority/provider owner.
- structural labels must come from canonical data authority.
- Saved Map requires explicit Parent/Controller authority before any storage/access design.
- non-prerequisite Overlay providers require explicit source authority.
- assurance/test owner must own controlled evidence fixtures and ~1024 recapture.

## 9. Closure requirements

A finding is not closed by code alone. Closure requires:
1. exact successor SHA;
2. direct runtime state;
3. source/provenance trace where semantic truth is involved;
4. 1440 and ~1024 when relevant;
5. RTL/Bidi and keyboard review;
6. zero material runtime errors;
7. no unauthorized cross-surface mutation;
8. Controller B adjudication.

## 10. Explicit non-authorization

This document does not:
- authorize any writer;
- authorize implementation;
- authorize Saved Map;
- authorize schema/migration creation;
- authorize shared shell edits;
- authorize acceptance/freeze/merge/release/deploy.
