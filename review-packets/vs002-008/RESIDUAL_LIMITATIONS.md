# Residual limitations

- Review candidate only; no self-approval or production policy approval.
- Browser screenshots and live console/network/overflow/focus/zoom observations are blocked by the execution approval environment.
- Docker is unavailable; no Docker runtime result is claimed.
- gitleaks is unavailable; the fallback secret scan is narrower.
- Synthetic local request only: no live HTTP target, CORS, cookie, CSP, OAuth, external middleware, browser policy, connector, scanner or public API claim.
- One bounded endpoint, Knowledge Unit, practice, scenario and policy family; no generic attack or policy engine.
- True simultaneous duplicate HTTP pressure is not forced; PostgreSQL uniqueness and duplicate-race recovery are implemented and sequential request equivalence/conflict is tested.
- The external reviewer must decide approval and deferred browser-policy scope.
