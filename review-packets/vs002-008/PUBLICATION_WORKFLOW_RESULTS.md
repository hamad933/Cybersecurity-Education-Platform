# Publication workflow results

PASS. VS-002 uses real authenticated routes for restore, optimistic update, submit, return with bounded rationale, approve and publish. Publication selects the required baseline and claims by Knowledge Unit. Published revisions are immutable and restore creates a new draft inheriting authority and provenance. Prose rejects active script/event/scheme content; registered code/request/response/log blocks retain examples as inert text.
