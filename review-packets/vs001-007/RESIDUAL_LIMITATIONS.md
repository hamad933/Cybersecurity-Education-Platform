# Residual limitations

1. `DOCKER_RUNTIME_UNAVAILABLE`: container image build/start/restart/history still requires a Docker-capable review host.
2. The authorization evaluator is a narrow educational subset, not Windows, AccessCheck, kernel parity, or a production decision engine.
3. Privileges, inheritance, conditional/object ACEs, integrity labels, restricted tokens, claims, central policies, share/remote behavior, and non-FILE mappings are unsupported.
4. All evidence is `SIMULATED`; no Real-Lab Validation or transfer claim was performed.
5. The mastery rule is versioned and provisional pending external human review.
6. The deterministic fallback secret scanner is narrower than gitleaks, which was unavailable.
7. Docker and the in-app Browser connector were unavailable; native locked runtimes and local Chrome/CDP provided honestly labeled fallback evidence.
8. The repeated PHPUnit warning is caused by the deliberately absent `.env` file; explicit test environment values were used and all tests passed.

No limitation is hidden as a pass, and none authorizes Task-008 or future slices.
