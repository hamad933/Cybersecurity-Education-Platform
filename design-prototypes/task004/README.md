# Task 004 UX Design Proof

Open `index.html` directly in a local browser. No server, build, package manager, public Internet, external font, CDN, framework, or dependency is required.

Use the global navigation or URL hashes documented in `screens/ROUTES.md`. Interactions are intentionally ephemeral UI demonstrations: lesson toggle/add/reorder, VS-001 simulated-result reveal/reset/replay, Scenario Studio validation notice, evidence review notice, and Manual AI import/review stage. They do not process sources, call AI, execute commands, award mastery, publish content, or mutate data.

Label: **DESIGN PROOF — NOT IMPLEMENTED PRODUCT**.

